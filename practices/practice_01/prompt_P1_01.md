<span id="prompt">**PROMPT:**</span>
-
opencode run "Посмотри PR и найди проблемы. Смотри только в TRAINING_PR.diff, больше ни в какие файлы" \ --file practices/practice_01/TRAINING_PR.diff

<span id="output">**OUTPUT:**</span>
-
Findings (по важности)

1. app/api.py (стр. 35–38): отсутствие валидации входного тела и возможный KeyError
- Код: return review_service.review(payload["diff"])
- Проблема: при отсутствии поля "diff" в теле запроса будет поднят KeyError и 500 вместо ожидаемого 422.
- Риски: нестабильность API, некорректные ответы для клиентов.
- Рекомендация: ввести Pydantic-модель запроса с обязательным полем diff или явно валидировать и выбрасывать HTTPException 422.

2. app/api.py (стр. 35–38) и app/review_service.py (стр. 19–22): использование dict[...] требует Python 3.9+
- Код: аннотации вида dict[str, str].
- Проблема: при Python < 3.9 это упадёт на импорт.
- Риски: несовместимость окружений.
- Рекомендация: либо гарантировать python>=3.9, либо заменить на typing.Dict[str, str] (или включить from __future__ import annotations в 3.8+ и использовать PEP 585 осторожно).

3. app/api.py (стр. 35–38): схема ответа и документация OpenAPI
- Проблема: возвращается произвольный dict без response_model, документация будет неточной («object» без описания полей).
- Риски: ухудшение DX, сложнее автогенерировать клиентов.
- Рекомендация: ввести Pydantic-модель ответа (например, ReviewResponse { comment: str }) и указать response_model.

4. app/api.py (стр. 35–38): отсутствие обработки ошибок LLM
- Проблема: любые исключения из review_service.review/LLM приведут к 500.
- Риски: нестабильность, отсутствие предсказуемого контракта.
- Рекомендация: оборачивать вызов в try/except и маппить на осмысленные коды (например, 502/503) с деталью; логировать.

5. app/api.py (стр. 35–38) и app/review_service.py (стр. 19–22): отсутствие ограничений на размер diff
- Проблема: большой diff напрямую уходит в LLM.
- Риски: DoS по времени/стоимости, превышение контекста модели, таймауты.
- Рекомендация: ввести ограничения (max length), усечение/суммаризацию до лимита, возможный 413 при превышении.

6. app/review_service.py (стр. 19–22): смешение доменной логики и формы API-ответа
- Код: return {"comment": answer}
- Проблема: сервис навязывает структуру API. Это затрудняет переиспользование сервиса.
- Рекомендация: сервис возвращает строку или доменную модель; формирование DTO оставить уровню API.

7. app/review_service.py (стр. 19–22): слабый промпт и отсутствие экранирования входа
- Код: f"Review this pull request and find problems:\n{diff}"
- Проблема: промпт-инъекция/сбивание инструкций, низкий контроль формата ответа.
- Риски: непредсказуемый выход, сложно парсить, качество результата.
- Рекомендация: чёткие инструкции по формату (список, приоритет, ссылки на строки), экранирование/обрамление diff (например, в кодблок), системные ограничения (лимит пунктов).

8. Тесты: отсутствуют новые тесты под добавленный эндпоинт и сервисную логику
- Риски: регрессии и неочевидные 500 при невалидном теле.
- Рекомендация: добавить тесты на 200 (валидный diff), 422 (нет diff/пустой), обработку ошибок LLM, кейс с большим diff.

Открытые вопросы

- Целевая версия Python в проекте? Если 3.8 — нужен typing.Dict или повышение версии.
- Требуемый контракт API: достаточно ли одного поля comment, или нужен структурированный список findings?
- Допустимый максимальный размер diff? Нужны ли усечение/суммаризация?
- Есть ли требования по локализации ответа (ru/en) и по стабильному формату для клиентов?
- Нужна ли аутентификация/авторизация на /api/reviews?

Краткое резюме изменений, которые стоит внести

1. Ввести модели Pydantic:
- ReviewRequest { diff: str } с ограничением длины.
- ReviewResponse { comment: str } и указать response_model в роуте.

2. Починить обработку невалидного тела:
- Либо Pydantic обеспечит 422, либо явная проверка и HTTPException 422.

3. Совместимость типов:
- Заменить dict[str, str] на typing.Dict[str, str] или зафиксировать python>=3.9.

4. Обработка ошибок LLM:
- try/except вокруг вызова сервиса с маппингом на 502/503 и логированием.

5. Улучшить промпт:
- Экранировать diff, задать формат и ограничения ответа; по возможности сокращать длинные дифы.

6. Разделить ответственность:
- Пусть ReviewService.review возвращает строку; формирование {"comment": ...} делать в API.

7. Добавить тесты для позитивного и негативных сценариев, включая большие входы.